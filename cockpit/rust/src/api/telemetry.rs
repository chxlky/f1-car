use telemetry::{CarIdentity, CarPhysics, ClientMessage, ServerMessage};

pub fn a_dummy_function_to_make_frb_generate_the_types(
    _identity: CarIdentity,
    _physics: CarPhysics,
    _client_message: ClientMessage,
    _server_message: ServerMessage,
) {
    unimplemented!();
}
